---
const { title } = Astro.props;
---

<div class="p-6 bg-white/3 border border-white/4 rounded">
  <div class="flex flex-col gap-6">
    {title ? <p class="font-semibold">{title}</p> : ""}
    <slot />
  </div>
</div>
