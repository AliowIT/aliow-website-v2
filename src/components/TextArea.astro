---
const {
  id,
  label,
  required = false,
  autocomplete = "",
  maxlength = 2000,
  rows = 5,
  feedback = "",
  class: extra = "",
  ...rest
} = Astro.props;
---

<div class="flex flex-col gap-2">
  <!-- Label -->
  {
    label && (
      <label
        for={id}
        class="font-semibold"
      >
        {label}
      </label>
    )
  }

  <!-- Textarea -->
  <textarea
    id={id}
    name={id}
    required={required}
    autocomplete={autocomplete}
    maxlength={maxlength}
    rows={rows}
    aria-describedby={feedback ? `${id}-feedback` : undefined}
    class={`form-textarea ${extra}`}
    {...rest}
  ></textarea>

  <!-- Feedback message -->
  {
    feedback && (
      <div
        id={`${id}-feedback`}
        class="empty-feedback invalid-feedback text-red-400 text-sm"
      >
        {feedback}
      </div>
    )
  }
</div>
