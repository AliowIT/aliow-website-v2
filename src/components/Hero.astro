---
import ButtonLink from "./ButtonLink.astro";
import video from "../assets/hero-background.mp4"
---

<section
  class="relative bg-[url(../assets/hero-background.png)] bg-cover bg-center bg-opacity-10 h-screen pt-20 p-6 text-white"
>
<!--<video class="left-0 top-0 absolute object-cover"  autoplay muted loop>
      <source webkit-playsinline="true"
        src={video}
        type="video/mp4"
      />
      Your browser does not support the video tag.
    </video> -->
  <div
    class="bg-primary/75 left-0 top-0 absolute h-full w-full backdrop-blur-xs"
  >
  </div>
  <div class="relative max-w-7xl mx-auto h-full flex items-center">
    <div class="flex flex-col gap-y-16">
      <h1 class="font-bold text-3xl sm:text-5xl lg:text-6xl">
        From vision to value:<br />low-code strategies<br />that work
      </h1>
      <ButtonLink href="#contact">Get in touch</ButtonLink>
    </div>
  </div>
</section>
